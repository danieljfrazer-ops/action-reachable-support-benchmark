# Prompt: D-9 verification and assessment (identical for Codex and Gemini)

Work only in docs/archive/red-team/. Do not edit any existing file; create files only inside a new folder review-d9-<yourname>/. Today is 7 September 2026.

CONTEXT. Round 3 (your review3-<yourname>/ and the other two) refuted the operationalisation of decision D-8 while confirming its direction. Decision D-9 in decisions-required.md proposes the replacement. Read: decisions-required.md (D-9.1 to D-9.5), round3-adjudication-and-tally.md (sections 1 and 4), stage-0a-contract-v3.4.md (sections 0, B, C, G, H), interface-spec-v3.md, sequential-ibd-spec.md (draft 2, the statistic being replaced), and review3-claude-opus/findings.md rows R3-1 to R3-4 plus review3-claude-opus/sim_d8.py (the ONLY existing simulation of the proposed statistic; it is single-model evidence and is what you are asked to reproduce or refute).

TASK 1 (mandatory, executable): INDEPENDENTLY REPRODUCE OR REFUTE D-9.1.
Build your own simulation (reuse your own review3 infrastructure; do not import from review3-claude-opus/) of a contract-B family-L instance at registry values (N_b=4, N_d=2, N_w=4, K=2, N_x in {10, 30}, f_conf=0.5, rho_u=0.8, noise scales from section 0, policy loaded on u so that the rho_min >= 0.4 witness is met, saturation < 0.05, complete loss of actuator 0 at t=1000 in a 2000-step episode, 5 percent block-periodic probing with reservoir_0=0, probes drawn i.i.d. from ±e_k at magnitude 1.0).
Implement BOTH statistics on the same probes, same window (trailing 500 steps), same isotonic calibration protocol (40 episodes per cell, half event-carrying):
  (A) draft-2 statistic: per (channel, horizon) Mann-Whitney z of |increments| after probes vs matched task-policy null increments; a_c = signed max over horizons of |z|.
  (B) D-9.1 statistic: per (channel, horizon h, actuator k) rank-sum z of the SIGNED increments o_c(t+h) - o_c(t) after probes that used +e_k against those after probes that used -e_k (no task-policy null at all); a_c = max over (k, h) of |z|.
Comparator: channel-agnostic linear predictor fitted on fault-free in-distribution data; per-channel support score = two-sided standardised innovation-shift statistic (state your exact formula); arm-specific isotonic calibrator by the same protocol. Also run the D-7a probed comparator (same CUSUM on streams carrying the identical probe injections).
Report, for confounder present AND absent, at offsets 200/500/1000, with >= 30 scored seeds and a no-event control: threshold-free AUC and AUPRC of each arm's support score against S^obs,eps; F1 at threshold 0.5 and at the best operating point chosen on the calibration split; the mean a_c by channel class (direct action children, reachable-indirect incl. downstream d, non-reachable) for both statistics so the sign inversion claim in R3-1 can be checked directly; the ablation W_u=0. State whether (B) reproduces R3-1's numbers within reason (AUC about 0.75 at 500 and 0.83 at 1000, confounder-insensitive) and whether (B) beats the comparator on the ranking metric under confounding by a margin that would survive a delta of 0.10 AUPRC. Save as review-d9-<yourname>/sim_d9.py with captured output. Keep runtime under about 10 minutes; reduce seeds if needed and say so.

TASK 2: ASSESS D-9.2 to D-9.5 on their merits. For each: valid | invalid | judgment call, with one paragraph of reasoning and any counter-proposal. Specifically attack: whether AUPRC is the right primary given class imbalance and copies/padding channels; whether the re-derived decision rules (simple confounding contrast within regime, R0-present positive control, R0-absent TOST non-superiority, interaction descriptive) are coherent and what the smallest effects of interest should be in AUPRC units; what a normative comparator spec must contain to make the headline reproducible; and whether "supervised calibration plus online estimation" is an honest task statement or whether the calibration protocol should change.

TASK 3: Attempt at least one new mutant the gate does not reject (executable-proofs/gate/; run python3 run_gate.py with a Python 3.12 / numpy 2.4.4 environment as the runner now enforces).

OUTPUT: review-d9-<yourname>/findings.md with (1) a one-line verdict per D-9 part (adopt | adopt with changes | reject), (2) the reproduction table for Task 1, (3) findings in the usual rubric (ID D9-<yourname>-n, severity, claim attacked, evidence, proposed fix, fix type), (4) attacks that failed, (5) a final paragraph: is the interventional arm now demonstrably viable at a 5 percent budget, yes or no, and what single change would most raise your confidence. Mark opinion as opinion. Cite the IBD paper (arXiv 2603.18257) where relied on.
