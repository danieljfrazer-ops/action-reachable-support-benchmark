# Codex round-2 findings — frozen version `d23960e6da441de7`

Review date: 6 September 2026. I treated `roadmap-v4.md` only as amended by `roadmap-v4.1-amendments.md`, and used only `stage-0a-contract-v3.1.md`, `interface-spec-v2.md`, `confirmation-design.csv`, and `executable-proofs/gate/` as the reviewed specification. No external source is needed for the findings below: they follow from the frozen artefacts, executed counterexamples, or elementary sampling calculations. Statements identified as opinion are judgment calls.

The documented clean-environment command completed with exit code 0 under Python 3.14.7: 17 assertion tests passed and the eight registered mutants were rejected. I separately executed contract C4 at `h=3, tau=1` with a rectangular action map and obtained `open_loop_response = [0.33, 0.06]`, exactly equal to `A^(h-1-tau) B a`; the corresponding Jacobian was `[[0.825], [0.15]]`.

## Findings

| ID | Severity | Claim attacked | Evidence | Proposed fix | Fix type |
|---|---|---|---|---|---|
| R2-CX-01 | **High** | The gate covers C1 “structural reachability over all latents,” and all executable reviewer mutants are killed. | A new mutant in `review2-codex/surviving_mutant_ad_omission.py` deletes the entire `A_d` block from `build_adjacency`. It is wrong whenever a directly reached downstream component propagates to another downstream component. It nevertheless exits 0 with **17 passed, 0 failed** because K10 has only one `d` component and directly reaches it through `C_d`; no test requires a path through `A_d`. The coverage matrix's C1 “covered” label is therefore false. | Add a two-component downstream chain with `C_d: b0 -> d0`, `A_d: d0 -> d1`, and assertions that `d0` first appears at `h=tau+2` and `d1` at `h=tau+3`; register this mutant in `mutants.py`. | test |
| R2-CX-02 | **High** | The frozen instruction set unambiguously directs implementers to v3.1/v2. | `roadmap-v4.1-amendments.md:3,34-36` names contract v3.1 and interface v2, but the still-normative `roadmap-v4.md:3` tells an implementing agent to read `stage-0a-contract-v3.md` and `interface-spec.md`. Contract v3.1 itself repeats the stale interface in E6 (`:103`) and deliverables (`:135`). “Stands except as amended” does not explicitly replace all three instructions. An agent following the bold “nothing else” sentence can implement superseded semantics. | Add an explicit amendment replacing roadmap v4 line 3, contract E6, and contract J with the exact frozen file list. Prefer a one-file manifest with hashes that `run_gate.py` checks. | prose |
| R2-CX-03 | **High** | The closed-loop spectral-radius rejection test certifies stability for all SCM cells, including delayed cells. | Contract `B` uses `a_(t-tau)` and the design includes `tau=2`, but `stage-0a-contract-v3.1.md:29,61` checks only `rho(A_b + B W_o diag(gain*avail) Assign_b)`. That is the zero-delay body-only matrix. With delay, the state must be augmented by the action-delay queue; if the policy observes `d`, the `d` dynamics and `b -> d -> observation -> action` feedback must also be included. A zero-delay body radius below 0.98 does not imply stability of that augmented system. This is a mathematical counterexample class, not an empirical tolerance issue. | Construct the exact augmented closed-loop transition matrix for each delay and all policy-observed dynamic latents, and reject on its spectral radius in family L. For family N, certify the actual delayed closed loop empirically as well as the open-loop chains. Add a mutant that applies the old body-only check. | test |
| R2-CX-04 | **High** | At least 20,000 stationary steps can establish `ARL_0=1000` within a ±10% tolerance for threshold matching. | Under the favourable independent rare-alarm approximation, 20,000 steps yield only about 20 false alarms. The count's relative standard error is `1/sqrt(20)=22.4%`. A direct Poisson calculation gives only about 0.339 probability that the count falls in the range corresponding approximately to ±10% ARL (19–22 events), even when the true ARL is exactly 1000. Dependence and threshold search can worsen this. The stated tolerance is substantially tighter than the calibration measurement. | Define a precision rule rather than a fixed minimum: e.g. continue calibration until a predeclared confidence interval for ARL lies inside the matching band, with a large maximum budget and censoring rule. At minimum target enough independent renewal intervals for the desired relative precision and validate coverage by simulation. | decision |
| R2-CX-05 | **High** | Excluding a method that cannot attain the target ARL is a fair resolution of calibration failure. | Contract `:24` labels such a run `not calibratable` and excludes it from the primary. Calibratability is method-dependent, so this can remove a poorly behaved comparator or treatment after seeing its attainable operating range and change the estimand. It also conflicts with `:114`, which says disjoint operating curves are reported as a partial order rather than forced into a scalar. | Freeze the estimand for failure: report the method as failing the matched-ARL endpoint, retain it in a predeclared partial-order/feasibility result, and forbid silently recomputing the primary on the surviving methods. State the paper-level decision if either confirmatory method is not calibratable in a required cell. | decision |
| R2-CX-06 | **High** | `confirmation-design.csv` unambiguously contains every observation contributing to the claim-gating interaction `I`. | Amendment E1 makes the absent/present 2x2 interaction a necessary confirmatory sign gate. In the CSV, all confounder-present seq-IBD/CUSUM rows have `role=primary`, while all absent rows have `role=diagnostic_2x2`. An implementation that correctly filters primary rows by `role` cannot compute `I`; one that includes diagnostic rows makes “diagnostic” data part of a confirmatory claim. Amendment E2's phrase “primary estimand per (environment, regime, confounder)” deepens the mismatch. | Introduce explicit roles such as `confirmatory_effect` and `confirmatory_interaction`, freeze the exact row selector for every reported estimand, and test that `I` consumes the intended four cells and no descriptive rows. | decision |
| R2-CX-07 | **High** | The adopted co-primary “confounded-channel false support” is well-defined as mean `p_c` on distractor channels at offsets 10, 50, and 200. | Only `f_conf=0.5` of distractors are confounded (`:27,99`), but the metric averages all distractor channels (`:113`), so it is not the named confounded-channel endpoint and its value is diluted by the unconfounded half. In addition, early termination is defined as a competing outcome for HPDT, but no rule defines `p_c` at later offsets after termination. Different imputation or complete-case choices can reverse a co-primary result. | Define the channel set explicitly (oracle-labelled confounded distractors, with unconfounded distractors a separate negative control), define per-event/per-channel weighting, and freeze handling of unavailable post-termination offsets. If oracle identity is considered too privileged for a primary metric, rename the endpoint and report strata separately. | decision |
| R2-CX-08 | **Medium** | Environment dependencies are pinned in `requirements.txt`. | Amendment E9 says “requirements pinned,” but the file contains only `numpy>=1.26` and `pytest>=8`. The fresh run installed NumPy 2.5.3 and pytest 9.1.1 under Python 3.14.7, whereas the stored gate output records NumPy 2.4.4 under Python 3.12. This is reproducible only by luck, not by the declared lockfile hash. | Pin the interpreter range and exact dependency versions, preferably with hashes; make the gate verify the resolved environment/lock hash. | test |
| R2-CX-09 | **Medium** | All normative thresholds are declared in the constants registry. | Faithfulness E1b uses `epsilon_faith` (`:62`), but that symbol is absent from the constants registry and its relation to operational `epsilon=0.05` is not defined. Two conforming implementations can accept different generators. | Add `epsilon_faith` with units and a frozen value or explicitly define it as a function of `epsilon`; add boundary tests immediately below, equal to, and above it. | prose |
| R2-CX-10 | **Medium** | The co-primary and confounded fraction are frozen decisions in version 2. | Amendment E7 says both defaults are adopted. Contract `:27` still says Daniel may change `f_conf`, and `:113` says the co-primary is “default adopted pending Daniel.” That is incompatible with a frozen confirmation design unless change control is invoked. | Replace both provisional phrases with the adopted values, or explicitly classify them as unresolved and block confirmation until Daniel signs them. | decision |
| R2-CX-11 | **Medium** | R0 and R1 residual models are specified tightly enough to reproduce the confirmatory comparison. | Contract `:107` leaves “+10 percent on the diagonal” ambiguous for rectangular `B_t`, does not say additive versus multiplicative perturbation or how zeros are handled, gives no order/fitting rule for the T2 linear autoregression, and defines T2 R0 as an MLP “trained to convergence” without architecture, optimiser, stopping rule, Ljung–Box lags, significance level, or multiplicity rule. These choices can materially determine CUSUM innovations and hence the primary contrast. | Add a machine-readable misspecification/training registry: exact transforms for each matrix shape, model orders, architectures, optimiser and seeds, stopping rule, whiteness test parameters, and a failure rule. Hash it into every ledger row. | prose |
| R2-CX-12 | **Medium** | Phase 0R's revised gate scope is already executable and ready for sign-off. | Amendment E10 includes `aggregate_primary()`, a coverage simulation, and interface-v2 acceptance stubs in Phase 0R. `interface-spec-v2.md:19` calls `aggregate_primary()` a reference implementation, but `contract_ref.py` has none and `coverage-matrix.md:17` correctly marks aggregation and coverage simulation uncovered. This is honestly disclosed, but the frozen artefacts cannot yet validate the principal estimand. **Opinion:** this blocks 0R sign-off, not continued implementation. | Implement and test aggregation against a hand-worked subset of the CSV, including role selection and missing cells; add the coverage simulation and interface acceptance stubs before Daniel signs 0R. | test |

## New-category statement

Yes. At least three findings are in categories not present in round 1: **rare-event calibration precision** (R2-CX-04), **delayed/full-state closed-loop stability mathematics** rather than merely a missing stability check (R2-CX-03), and **co-primary population plus post-termination missingness** introduced by the v4.1 endpoint (R2-CX-07). R2-CX-06 is also a new design-table/claim-gate linkage problem. R2-CX-01 is a new surviving mutant but belongs to the round-1 mutation-adequacy category.

## Attacks that failed

| ID | Severity | Claim attacked | Evidence | Proposed fix | Fix type |
|---|---|---|---|---|---|
| R2-AF-01 | — | The documented clean gate might fail or merely print success. | In a fresh `.venv`, the required command exited 0; pytest reported 17 passed, and `mutants.py` rejected all eight registered mutants. `test_proof_theatre.py` also guards against print-only tests. Attack failed, subject to R2-CX-01 and uncovered items already disclosed. | None. | test |
| R2-AF-02 | — | The corrected delayed Jacobian equation may still have an exponent or scaling error. | Direct execution at `h=3, tau=1` produced equality between simulation and `A^(h-1-tau) B a`; grid tests also cover tau 0–3 and rectangular `B`. Attack failed. | None. | test |
| R2-AF-03 | — | The confirmation CSV may contain duplicates, omit a seed, or disagree on frozen numeric fields. | Programmatic parsing found 1,764 unique rows, no duplicate design keys, seeds 0–9 for the two confirmatory estimators, three seeds for descriptive estimators, and no deviations in `probe_budget_fraction=.05`, `ARL_0=1000`, `H_det=200`, or `s_change_certified=REQUIRED_TRUE`. Attack failed, apart from the role/estimand ambiguity in R2-CX-06. | None. | test |
| R2-AF-04 | — | The v2 estimator interface may still omit applied actions, probe identity, termination semantics, or probe sequences. | `interface-spec-v2.md:5-12` supplies Gymnasium-style termination fields, immutable transitions containing `applied_action` and `probe_flag`, and sequence-valued probe requests. Attack failed. | None. | prose |
| R2-AF-05 | — | The interaction and smallest effects might remain post-pilot degrees of freedom. | Amendments E1 and E4 define the interaction sign gate and freeze `delta=20`, `delta0=10`; the pilot may alter only seed count. Attack failed. | None. | decision |

