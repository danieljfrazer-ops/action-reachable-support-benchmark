# Decisions required from Daniel

Updated 8 September 2026 after rounds 6 and 7. **D-1 to D-12 decided; D-13 open after round 8 (`round8-adjudication.md`).** Round 8 found the D-12 premise false (passive identifiability is lost only on a knife-edge; what fails on an open set is the no-latent model class), the E2 confuser dissociation as the one sign-fixed prediction, the adaptation-under-confounding sign disputed, and option C's novelty a 3, the same as option B. Round 6 (`round6-adjudication-and-tally.md`): three models reproduced to three decimals; the D-11.1a primary cannot see the confounder by construction (author's error); the passive comparator wins the re-posed task; two findings are reproducible across rounds 5 and 6 (interventions buy attribution, not adaptation). D-11 history follows.

## D-13 (open)

| # | Decision | Options | Recommendation |
|---|---|---|---|
| D-13 | **Direction after round 8.** No path above novelty 3 survived eight rounds. | (A) Stop building. Write a technical report of rounds 1 to 8: the online reproduction of IBD's attribution result, the passive-wins adaptation result with its construction caveats, the round-8 identifiability picture, the generator and gate as reusable artefacts, and the methodological lessons (a primary the treatment cannot reach; a change score that ignores the confounder; a lost coordinate chosen by dominance). About a day of writing; archived or posted as a report, not submitted as a paper. (B) One boxed execution round on the existing plant with the round-6 corrections and a topology-repaired event, aiming at a benchmark-and-findings paper at novelty 3 (TMLR, CoLLAs, a NeurIPS evaluations track); the headline must be conditional. (C) The body-confounded plant with the E2 confuser dissociation as primary and a latent-modelling passive third arm; novelty 3, certification rewrite, adaptation sign disputed, highest cost. | **A**, with B available if Daniel wants a publication at the 3 level. Not C: it costs the most for the same ceiling and its premise did not survive. Daniel's own criterion is genuinely novel work; eight rounds say this question does not yield it. |

## D-12 (decided 9 September 2026: derive, then choose; round 8 done)

| # | Decision | Options | Recommendation |
|---|---|---|---|
| D-12 | **Direction after round 6.** | (A) Stop building; write the D-10 fallback as a benchmark-and-findings paper: "interventions buy attribution, not adaptation", with the passive-wins result as the negative headline. (B) Re-pose once more: two named endpoints, an **attribution** endpoint over the full channel set with confounded distractors as the hard negatives (carries the superiority claim; round 5 and 6 evidence +0.16 to +0.24 AUC) and an **adaptation** endpoint over the pre-event support (reported as a finding, passive expected to win); event redesigned by topology so n_lost ≥ 2 with the lost channel balanced over its pre-event rank; the round-6 defects fixed; freeze version 7; one more execution round. (C) Change the plant so the confounder reaches controllable channels (Gemini GM6-01 fix), making adaptation under confounding a real test; invalidates the current certification design. | **Round 7 verdicts:** Codex B, Gemini B, Claude A-scoped-down with a derive-first middle path; three of three reject A as worded and score option C's question highest (3.5 to 4) and the negative result lowest (1). **Adjudicated recommendation: derive, then choose.** (1) Cross-model prose round on the identifiability of loss of control under a confounder that reaches the body (passive information sets versus sign-randomised probing, population limit, linear-Gaussian). (2) If passive is non-identifiable across a non-knife-edge range and probing identifies: freeze C with attribution and adaptation as two named endpoints under one confounding regime, one execution round. Otherwise: B boxed to one execution round with the conditional claim. (3) If the single round fails: A as a narrow technical report. The author's earlier binary prior (B if novel else A) is withdrawn as a false dilemma (Gemini). |

Sub-decisions that apply under B or C, adopted as safe corrections when D-12 is decided (three-of-three in round 6): interval estimator fixed in the contract (percentile cluster bootstrap over instance means, two-sided, resample count and seed declared); the 0.85 floor replaced by the per-episode probability that every lost channel ranks strictly below every retained channel, with the floor stated on that proportion; perturbation rows materialised in the matrix; arm-namespaced episode-id ranges sized to the cap and gated against the generator's reserved ids; interface v6 with a per-instance fitted-model object and an explicit episode reset; `instance_index` and `certified_sub_seed` separated in the matrix; generator mutants registrable in `mutants.py` with fixtures for the three survivors; Δ_c's null standardised per channel on the fault-free split; λ_rel disclosed as development-selected; the descriptive alarm comparison scoped down by default under the per-instance ARL budget.

## D-11 (decided 7 September 2026) D-11: Daniel adopted all six recommendations on 7 September 2026 ("Adopt all six recommendations, proceed to Step 3"); encoded in `stage-0a-contract-v3.9.md` §L, `interface-spec-v5.md`, the gate (66 tests, 47 mutants), `confirmation_seeds.py` (commitment `89a262a9…`; the secret is at a file held privately by the author outside this repository). No decision is currently open. Round 5 met the convergence test and produced a design-level result (`round5-adjudication-and-tally.md` §1): the interventional arm is reproducible, the comparator's change statistic is defective, and the benchmark's event usually changes nothing, so the primary is mostly static. D-11 re-poses the task.

## D-11

| # | Decision | Options | Recommendation |
|---|---|---|---|
| D-11.1 | **Make the primary measure adaptation to change, not static reachability.** | (a) Certify CL-4 in the generator (reject any instance where losing the target actuator removes no channel) and define the primary AUC **over the pre-event support only**: which previously controllable channels remain controllable after the event. A fit-time constant then scores 0.5 by construction; full-channel AUC becomes secondary. (b) Keep full-channel AUC and only certify CL-4. | **(a).** It is the only option under which the benchmark measures what the working claim says. Both arms must then track change; the passive arm's change statistic becomes decisive rather than harmful. |
| D-11.2 | **Comparator change statistic.** The mean-shift term cannot see an actuator loss under a centred policy. | (a) Replace with a second-moment statistic: a score-type test on the change in action-residual covariance (equivalently the drop in action loading re-estimated on the trailing window against the fit-split loading), standardised; loading term retained for the secondary full-channel score. (b) Keep v2 and report the failure. | **(a)**, as the strongest passive reading; cross-model execution round before freezing. |
| D-11.3 | **Confirmation seeds.** Configuration seeds 0 to 9 are exhausted. | Reserve a disjoint held-out seed set now (hashed list held by you, not inspected until specs and constants are frozen); 0 to 9 remain development. | Adopt. |
| D-11.4 | **Family N.** The generator implements family L only; the matrix has family-N rows. | (a) Implement and certify family N in the generator now, keep it out of the confirmatory conjunction until reproduced cross-model; (b) scope Paper 1 confirmation to family L. | (a). |
| D-11.5 | **Alarm exposure.** The IBD warm-up starts its false-alarm clock at step 502; the comparator starts at reset. | Both arms count elapsed steps from reset; the warm-up prefix is charged to the arm that needs it (a longer run length is the honest price); harness alarm bookkeeping becomes timestamp-aware (persistence in the arm's declared unit, refractory in steps). | Adopt. |
| D-11.6 | **Fit hierarchy.** Per-instance fitting versus pooled across instances is undefined for every learned artefact. | Per-instance fitting on that instance's fault-free fit split; nothing pooled across instances; key (cell, instance_seed, arm) recorded in the ledger. | Adopt. |

Applied without asking (three-of-three, code or hygiene): deterministic noise key; c_min enforced after scaling and B density asserted; burn-in implemented; noise multiplier applied to every stream; CL-4 certification (event target chosen and certified in `draw_certified`); confounded-mask and CRN-independence tests; the generator mutants registered; cross-process determinism test; matrix rebuilt with instance and episode seeds and the current arm identifiers; contract cross-references corrected.

## After D-11 (done 7 Sep 2026)
Contract v3.9; comparator spec v3 and IBD draft 5; frozen version 6 `38d161e762a3de76`; round 6 executed by three models. Outcome in `round6-adjudication-and-tally.md`: exit condition unattainable as posed; D-12 above.
