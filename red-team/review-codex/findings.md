# Codex findings on frozen version `c197652c0d8e846b`

Reviewed on 6 September 2026 using only the frozen files named in the review request. Severity is **opinion**. The required command `python3 run_gate.py` was executed exactly as written; the contract’s delayed-response equation was also checked independently at `τ=2` on a rectangular system.

| ID | Severity | Claim attacked | Evidence (live sources where external) | Proposed fix | Fix type |
|---|---|---|---|---|---|
| CX-01 | **high** | The current gate rejects wrong implementations of the contract’s delayed open-loop response. | I wrote `review-codex/mutant_tau_cap.py`, which incorrectly maps every `τ≥1` to `τ=1`. The frozen tests exercise only `τ=0` and `τ=1`; under the NumPy-capable workspace interpreter the mutated suite reports **15 passed, 0 failed**. Contract v3 permits larger delays. Per the review instruction, a mutant that the gate does not reject is a high finding. | Add parameterised tests for `τ∈{0,1,2,3}`, including `h<τ+1`, `h=τ+1`, and later horizons, on both square and rectangular `B`. Require mutation testing to show the delay-cap mutant is killed. | test |
| CX-02 | **high** | The frozen gate runs via the mandated command `python3 run_gate.py`. | The exact command fails before collecting tests with `ModuleNotFoundError: No module named 'numpy'` because `python3` resolves to Homebrew Python 3.14 without NumPy. The stored `gate.output.txt` was therefore produced in a different environment. Running with the bundled NumPy-capable interpreter passes 15 tests, but that is not the frozen command. NumPy’s installation guide recommends a project/environment installation and explicit import verification: https://numpy.org/install | Add a locked environment (`pyproject.toml`/lockfile or a self-contained documented runner), make `run_gate.py` verify and print interpreter plus dependency versions, and ensure the exact documented command works from a clean checkout. Regenerate the signed output from that environment. | test |
| CX-03 | **high** | The one-page interface is sufficient for every Paper 1 estimator and can ground an independent black-box suite. | `Estimator.update(obs)` never receives the action actually applied, the preceding observation/transition, or a probe/ordinary-action marker. Sequential IBD needs to associate randomized interventions with responses; the forward predictor and CUSUM need action-conditioned innovations. `request_probe` tells the environment what to do but does not supply the realised action/transition back to a generic estimator. Implementations would need hidden shared state outside the frozen interface, defeating E6. | Replace it with an explicit transition API such as `update(prev_obs, applied_action, obs, terminated, truncated, probe_metadata)`, or define an immutable transition record. Specify timing at reset and after probes. Add a black-box estimator that demonstrably computes an action-conditioned residual using only the public interface. | decision |
| CX-04 | **high** | The oracle interface faithfully represents nonlinear `R` from contract C4. | C4 defines `R_{t,h}(a)` for every probe `a∈𝒜`, and `|𝒜|=2K`. For nonlinear family N, `R(-e_k)` need not equal `-R(e_k)`, so it cannot be compressed to a `K` axis. The interface nevertheless declares `R: float[N_b, K, |ℋ|]`, conflating nonlinear finite responses with the linear Jacobian. | Declare `R: float[N_b, |𝒜|, |ℋ|]` with a frozen probe ordering. If a Jacobian is also exposed, name it separately as `M: float[N_b,K,|ℋ|]` and limit exact equivalence to family L. Add a nonlinear asymmetry test. | prose |
| CX-05 | **high** | The coverage matrix covers C1 structural reachability and contract K’s downstream case. | C1 includes paths through `C_d` and K10 requires a downstream `d` channel inside `S` but outside body membership. `contract_ref.structural_reach(A,B,H)` models only body matrices `A,B`; it cannot represent `d`, `C_d`, `w`, or `x`. `test_gate.py` has no K10 test despite the contract saying every K case is tested. The coverage matrix marks C1 covered, so its uncovered count is understated. | Implement graph reachability over the full declarative SCM and test positive `a→b→d` plus negative `a↛w,x` paths at several horizons. Add K10 and change C1 to uncovered until it passes. Generate the coverage matrix from test metadata or validate that every named test exists. | test |
| CX-06 | **high** | The primary comparison is frozen at a reproducible matched false-alarm level and declared intervention cost. | `ARL_0` has no numerical value in the constants registry or CSV. `probe_budget=0.05` has no unit or semantics (probability per step, action-deviation norm, fraction of steps, or regret). A threshold policy string cannot make either quantity reproducible. If an estimator cannot attain the target ARL on calibration data, no fallback is specified. | Freeze `ARL_0`, its estimation window and tolerance; define probe budget units and accounting; specify what happens when the calibrated threshold cannot attain ARL₀. Put these values in a machine-readable registry referenced by every CSV row. | decision |
| CX-07 | **high** | The 2×2 diagnostic makes an R1 advantage attributable to confounding rather than generic model misspecification. | The CSV contains all four combinations of clean/misspecified × confounder absent/present, but the plan defines no interaction estimand or gate. The confirmatory rule tests IBD superiority only with confounding present. Misspecification alone could generate the same CUSUM failure, yet the efficacy claim could pass. | Predeclare an interaction contrast, e.g. the change in `Δ` from confounder absent to present under R1 minus the same change under R0, with uncertainty and a minimum effect or at least a sign gate. Require R1 superiority to be accompanied by evidence that confounding adds failure beyond misspecification alone. | decision |
| CX-08 | **high** | `RMDT = mean(min(delay,H_det))` correctly handles every event labelled “censored.” | The formula is a valid capped composite outcome when a miss is deliberately assigned `H_det`. It is not standard restricted-mean handling of an event merely right-censored early by the next change or episode end: such a case only establishes that detection time exceeds the censoring time, not that it equals `H_det`. Treating all early censoring as `H_det` can bias schedules differently. Survival literature treats right-censoring as incomplete time-to-event information rather than an observed horizon-time event; see https://arxiv.org/abs/2306.16075. | Either (a) guarantee no next event or administrative end before `H_det`, (b) rename the metric “horizon-penalised detection time” and make early termination/miss a composite failure by decision, or (c) use a proper censored-time estimator and competing-risk analysis. Add tests with censoring at several pre-horizon times. | decision |
| CX-09 | **high** | Family N is normatively specified and its “contractive” requirement is testable as stated. | The equation introduces `s`, `κ`, and `m`, but none has a value in the constants registry. More fundamentally, bounded trajectories from 32 initial states and agreement between two burn-ins do not certify contraction; a bounded nonlinear system can have multiple attractors or noncontractive dynamics. The test described can certify boundedness and one empirical stationarity check, not the stated mathematical property. | Choose and freeze `s,κ,m`. Either impose and verify a Lipschitz/Jacobian norm bound on the reachable set, or replace “contractive” with the weaker empirical boundedness/stationarity claim and add multi-chain convergence plus failure thresholds. | decision |
| CX-10 | **medium** | The frozen confirmation CSV operationalises the full five-baseline benchmark. | All 1,440 rows use only `seq_ibd` or `cusum_channel_agnostic`. Random, temporal correlation and forward-model residual are named Paper 1 baselines but have no confirmation rows or explicit statement that they are pilot/appendix-only. A benchmark paper can reasonably have one confirmatory pair, but the remaining advertised methods still need frozen evaluation coverage. | State which baselines are confirmatory versus descriptive and add a separate frozen descriptive matrix for the other three, possibly fewer seeds. If they are intentionally excluded from Paper 1 results, change the “Baselines (five)” claim. | decision |
| CX-11 | **medium** | The CSV uniquely defines how the primary effect is aggregated. | It crosses three distractor levels and, for SCMs, two action delays. The decision rule asks for one interval per family but does not specify pooling, averaging weights, worst-case gating, or separate strata. Different legitimate aggregations can change the result. `role=primary` identifies rows, not the estimand over rows. | Freeze a row-to-estimand aggregation specification: primary distractor/delay cell, equal-weight marginal mean, or worst-case rule. Record it as code plus a worked CSV example before confirmation. | decision |
| CX-12 | **medium** | Ten seeds support trustworthy 95% intervals “clustered by seed” across the large crossed design. | Reusing seeds across 144 rows per seed correctly motivates clustering, but there are only ten independent clusters and no bootstrap type, resample unit, or small-cluster correction is declared. Ordinary cluster bootstrap inference can be unreliable with few clusters; specialised wild/jackknife cluster methods are proposed for this regime: https://arxiv.org/abs/2301.04527. | Run simulation-based coverage checks under the frozen design. Predeclare the cluster bootstrap variant or use seed-level paired effects with a small-sample method; increase independent seeds if coverage is poor. | decision |
| CX-13 | **medium** | The interface supports the declared Gymnasium T2 environments, early termination, task regret and ledger outcomes. | `step(action)->obs` and `reset(seed)->obs` omit reward, `terminated`, `truncated`, and `info`. Yet contract G treats early termination as a competing outcome and allows task regret. Current Gymnasium returns `(obs,reward,terminated,truncated,info)` from `step` and `(obs,info)` from `reset`: https://gymnasium.farama.org/api/env/. A deliberately narrower benchmark API is possible, but then evaluator/ledger access to those fields must be specified. | Either adopt the Gymnasium return contract or define a separate evaluator-only transition record carrying reward and termination/truncation cause. Add black-box tests for 1000-step truncation, physical termination and regret logging. | decision |
| CX-14 | **medium** | `max_pairwise_corr` implements the normative maximum over valid action–distractor pairs. | The reference function returns NaN if **any** action or distractor column is constant, even when another generated witness pair is nonconstant and exceeds `ρ_min`. The contract requires a witness pair and rejects a constant probe, not necessarily an entire observational dataset with one unused action dimension. The current test covers only the all-constant-action case. | Define pair eligibility. Skip zero-variance non-witness pairs, require the named witness pair to have positive variance, and return NaN/fail only when no eligible pair exists or the witness is degenerate. Add a mixed constant/nonconstant rectangular case. | test |
| CX-15 | **medium** | Operational-label uncertainty is reproducibly certified by `n_oracle=4096` and a “99 percent interval.” | The contract does not define the interval estimator, multiplicity correction across components/horizons/probes, or desired label-error probability for a whole generated instance. Pointwise 99% intervals can have much lower simultaneous coverage when many labels are screened; resampling rejected near-threshold instances can also alter the generator distribution. | Freeze the interval construction and simultaneous error control, report the resampling rate, and cap/reject generator configurations whose ambiguity rate is excessive. Add calibration simulations for oracle-label error. | decision |
| CX-16 | **medium** | The SOEI values are practical thresholds that can be changed after the pilot because the variance is learned. | The roadmap says `δ=20` and `δ0=10` are provisional until pilot variance is known. Sampling variance should determine sample size and precision, not what effect is practically meaningful; lowering the SOEI to fit observed power would weaken the confirmatory claim after looking at data. | Freeze practical margins before outcome-bearing pilot results, or allow changes only from an external domain argument while keeping the original thresholds reported. Use pilot variance solely for seed count and precision planning. | decision |
| CX-17 | **low** | The contract’s out-of-band support queries are unambiguous and cannot affect the estimator or environment. | The interface exposes only `probe`, which appears to advance the environment and returns observations. It defines no evaluator-only “out-of-band” query used for F1 at fixed offsets. An implementer could update dynamics, consume RNG, or accidentally feed probe results to the estimator, changing the measured trajectory. | Define an evaluator-only snapshot/branch API with common random numbers, explicit no-update semantics and a test that state, RNG and estimator bytes are unchanged after scoring queries. | prose |
| CX-18 | **low** | The frozen design fully specifies R1 misspecification. | “Linear model on family N; drifted parameters on family L” does not specify which parameters drift, by how much, whether drift is known to the generator, or how T2 models are misspecified. The CSV has only the label `R1_misspecified`. This remains a researcher degree of freedom capable of controlling CUSUM failure severity. | Add a hashed misspecification registry with exact parameter transforms and T2 surrogate models; include mild/declared primary severity and hold other severities descriptive. | decision |

## Equation verification executed

I independently evaluated the family-L equation

`M_{t,h}=0` for `h≤τ`, otherwise `M_{t,h}=A_b^(h−1−τ)B`,

against explicit open-loop rollout using a `3×2` rectangular `B`, `τ=2`, and `h=1…5`. All five numerical responses matched, including zero response at `h=1,2` and first response at `h=3`. This supports the equation; it does **not** cure CX-01 because the frozen gate does not test `τ=2`.

## Gate execution record

- Required command: `python3 run_gate.py` → **failed before test collection** (`ModuleNotFoundError: numpy`).
- Workspace NumPy interpreter: `…/dependencies/python/bin/python3 run_gate.py` → **15 passed, 0 failed**.
- Mutated suite: `…/dependencies/python/bin/python3 review-codex/mutant_tau_cap.py` → **15 passed, 0 failed**.
