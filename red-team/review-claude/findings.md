# Review of frozen version c197652c0d8e846b (Claude)

Date: 6 September 2026. Scope: `roadmap-v4.md`, `stage-0a-contract-v3.md`, `interface-spec.md`, `confirmation-design.csv`, `executable-proofs/gate/`. Gate executed: 15 pass, 0 fail. Adversarial execution: `uncaught_mutants.py` (output in `uncaught_mutants.output.txt`). I authored the artefacts under review; findings below are against my own work and are not softened for that.

| ID | Sev | Claim attacked | Evidence | Proposed fix | Fix type |
|---|---|---|---|---|---|
| CL-1 | High | Contract C1 (structural support over all latents incl. downstream d via C_d) is implemented and tested | `contract_ref.structural_reach(A, B, H)` has no C_d, no d, w, or x; an oracle that labels every downstream component unreachable passes the gate (UM-1: 0 failures). Case K10 named in contract K has no test. | Extend the reference to a full block adjacency over z = [b; d; w; x]; add test K10 asserting d reachable and w, x not; add mutant M9 "oracle labels d unreachable" | test |
| CL-2 | High | C1 and C4 are consistent | C1 reachability ignores actuator delay τ. With τ = 1, H = 1 the reference returns support [True, True] while the C4 open-loop response is exactly zero (UM-2). S^latent and S^latent,ε disagree by construction on every delayed instance; E1b's "two derivations agree" would fail in 0A, but the gate today cannot see it. | Define C1 with delay: paths counted from h = τ + 1; add τ to `structural_reach`; test H ≤ τ gives empty support and H = τ + 1 gives B's rows | test |
| CL-3 | High | Mutants M2 and M5 are covered | `test_M2_rejected` and `test_M5_rejected` compare hard-coded arrays; replacing the mutant bodies with constants changes nothing (UM-3: 0 failures). `coverage-matrix.md` lists them as covered. | Reclassify M2, M5 as uncovered until the generator's event handler and oracle exist; the readiness criterion "uncovered count 0" is currently understated by two | test |
| CL-4 | Medium | The confirmatory event (complete actuator loss) produces an S^obs change in every primary cell | Contract D and test K4: with coupling, loss need not change S at H ≥ 2. `confirmation-design.csv` has no instance filter or column recording whether S changed; a cell with no true support change has undefined detection delay. | Add a normative instance constraint for primary cells: the lost actuator's column must reach ≥ 1 component with no alternative path within H; oracle certifies; CSV gains `s_change_certified` | prose + test |
| CL-5 | Medium | Regime R0 "clean, correct dynamics model" is defined for all four environments | For CartPole and Pendulum-v1 there is no correct parametric residual model; R0 is undefined on T2. | Define R0 on T2 as a residual model fitted on abundant fault-free in-distribution data with a pilot whiteness test on innovations; R1 as a linear model on the same data | prose |
| CL-6 | Medium | RMDT of α^S at matched ARL_0 discriminates the two methods on T2 | Single-actuator complete loss empties the support: every body channel stops responding, a floor-effect fault that both detectors catch easily. The confounding failure shows in p_c on distractor channels, not in alarm timing. | Make p_c on distractor channels at fixed offsets a co-primary outcome, or use K ≥ 2 SCM partial-support loss as the primary and T2 as a transfer check | decision |
| CL-7 | Medium | Interface spec oracle `R: float[N_b, K, |ℋ|]` matches contract C4 | C4 defines R over the probe set 𝒜 of 2K signed vectors; family N responses are not odd in a, so the sign cannot be folded. Shape loses half the information. | `R: float[N_b, 2K, |ℋ|]` indexed by 𝒜 | prose |
| CL-8 | Medium | Partial actuator loss is an R-only event | Contract D says S unchanged "which certified instances exclude", but certification (C2) happens at sampling; a small γ can push γ·e_j below ε legitimately, making the event an S change. | Add γ_min to the registry such that γ_min · e_j > ε on certified instances; re-certify after every event, not only at sampling (extend C2 accordingly) | prose + test |
| CL-9 | Medium | Stability constraints (spectral radius of A_b ≤ ρ_max) make the default-policy closed loop well behaved | Closed loop is A_b + B W_o diag(gain ⊙ avail) Assign_b before clipping; its radius is unconstrained. Trajectories are bounded by clipping but may saturate, which can make ρ_min unattainable and z̄ policy-dependent. | Constrain the closed-loop radius or W_o scale; certify ρ_min after sampling W_o, W_u; record saturation fraction | prose + test |
| CL-10 | Low | R1 decision rule "sign holds on both T2 environments" is operational | Undefined: point estimate, bound, or majority of seeds. | Define as point estimate > 0 with lower bound > −δ0 | prose |
| CL-11 | Low | `probe(action, steps)` supports sequential IBD | A single action held for `steps` is a constant probe; sequential IBD needs per-step randomised actions; repeated single-step calls work but cost accounting per call is unspecified. | `probe(actions[steps, K])` with cost = steps | prose |
| CL-12 | Low | Gate readiness is measured by exit code | `run_gate.py` returns non-zero on failure (correct) but `gate.output.txt` is a captured print; pytest is not installed. | State that the criterion is the runner's exit code; install pytest in 0R | prose |

## Attacks that failed
- The shared-cause distractor construction: explicit equations, no action parent of x; T-E2c/d tested with rectangular K ≠ N_x.
- Open-loop response and piecewise Jacobian: executed, invariant to the observation map, zero before delay; a reintroduced negative-power bug fails the gate.
- Gain in observed support: tested at 0, 0.05, −1.
- RMDT censoring: tested.
- Statistic at matched ARL_0: disjoint supports impossible by construction.
- Regime R2 removed from confirmation; R0 uses a non-superiority margin; comparator frozen; matrix reconstructible (1,440 rows).

## Summary
Three high findings, all in the gate I built earlier today: the reference covers body components only, ignores delay, and two "mutant" tests test nothing. The contract prose is closer to correct than its executable coverage; the readiness criterion of zero uncovered items is therefore further away than the coverage matrix states. None of this changes the direction. Adjudication of the other two reviews will follow in `adjudication.md` when they are present.
